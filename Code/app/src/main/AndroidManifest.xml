<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusServiceApp"
        tools:targetApi="33">

        <!-- Splash Activity - Launcher -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CampusServiceApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Activities -->
        <activity android:name=".ui.auth.LoginActivity" android:theme="@style/Theme.CampusServiceApp.Auth" />
        <activity android:name=".ui.auth.SignupActivity" android:theme="@style/Theme.CampusServiceApp.Auth" />
        <activity android:name=".ui.auth.ProfileSetupActivity" android:theme="@style/Theme.CampusServiceApp.Auth" />
        <activity android:name=".ui.auth.ForgotPasswordActivity" android:theme="@style/Theme.CampusServiceApp.Auth" />

        <!-- Main Activity -->
        <activity android:name=".MainActivity" />

        <!-- Feature Activities -->
        <activity android:name=".ui.chat.ChatActivity" />
        <activity android:name=".ui.notifications.NotificationsActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.profile.ViewProfileActivity" />
        <activity android:name=".ui.social.FriendRequestsActivity" />

    </application>

</manifest>